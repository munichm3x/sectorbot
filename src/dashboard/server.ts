// src/dashboard/server.ts
// Stub — real implementation in Sub-project B.
// This file exists so that the dynamic import in src/index.ts is correctly typed.
import type { Client } from 'discord.js';

export function startDashboard(_client: Client): void {
  // Implemented in Sub-project B
}
